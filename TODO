API to l7, sock? IPC? queue?
TCP
DHCP
IP:
	ip and tcp options
	fragmentation
	forward, NAT
UDP:
	get port
	connect, disconnect
ICMP
DNS

sock:
	dynamically allocate
	find sock when demultiplexing

sk_buff_list:
	full implementation: queue
pthread
	refact
	how many
