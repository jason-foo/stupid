TCP
IP:
	ip options
	fragmentation
	forward, NAT
ICMP
DHCP

timer for asynchronous events

API:
limitation of fifo
	PIPI_BUF
	open files number limitation
	IPC garbage collection

so replace fifo with?
   UNIX domain sockets with asynchronous I/O
   shared memory
